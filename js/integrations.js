/* ============================================================
   LUMÈS — Integrations (cookie banner, analytics)
   RÈGLE : zéro accès DOM au top-level
   ============================================================ */

var CONFIG = {
  GA_ID: 'G-XXXXXXXXXX'  // Remplacer par votre vrai ID Google Analytics
};

function initCookieBanner() {
  // Already handled in lumes.js initPage()
}
